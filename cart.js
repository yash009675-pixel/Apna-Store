const box=document.getElementById("cart");function readCart(){try{const d=JSON.parse(localStorage.getItem("apnaCart")||"[]");return Array.isArray(d)?d.filter(x=>x&&x.name).map(x=>({...x,price:Number(x.price)||0,qty:Math.max(1,Number(x.qty)||1)})):[]}catch{return[]}}function saveCart(c){localStorage.setItem("apnaCart",JSON.stringify(c))}function money(n){return "₹"+Number(n||0).toLocaleString("en-IN")}function draw(){const c=readCart();if(!c.length){box.innerHTML='<div class="empty-cart">Your bag is empty. <a href="shop.html">Start shopping →</a></div>';return}const subtotal=c.reduce((s,x)=>s+x.price*x.qty,0),shipping=subtotal>=999?0:49;box.innerHTML='<div class="cart-layout"><div class="cart-items">'+c.map((x,i)=>'<article class="cart-item"><div class="cart-thumb"><span>'+x.name+'</span></div><div class="cart-product"><h3>'+x.name+'</h3><p>'+(x.category||"Apna Store")+'</p><p>'+[x.size,x.color].filter(Boolean).join(" · ")+'</p><b>'+money(x.price)+'</b><div class="cart-controls"><button onclick="change('+i+',-1)">−</button><span>'+x.qty+'</span><button onclick="change('+i+',1)">+</button><button class="remove-btn" onclick="removeItem('+i+')">Remove</button></div></div></article>').join("")+'</div><aside class="cart-summary"><p class="eyebrow">SUMMARY</p><div><span>Subtotal</span><b>'+money(subtotal)+'</b></div><div><span>Delivery</span><b>'+(shipping?money(shipping):"FREE")+'</b></div><small>Free delivery on orders over ₹999.</small><div class="summary-total"><span>Total</span><b>'+money(subtotal+shipping)+'</b></div><a class="primary-btn" href="checkout.html">Proceed to checkout →</a></aside></div>'}async function change(i,d){const c=readCart();if(!c[i])return;const item=c[i];if(d>0&&item.productId){const {data:v}=await apnaSupabase.from("product_variants").select("stock").eq("id",item.variantId).maybeSingle();if(v&&item.qty+1>Number(v.stock)){alert("Only "+Number(v.stock)+" item(s) are available for this variant.");return}}item.qty+=d;if(item.qty<=0)c.splice(i,1);saveCart(c);draw()}function removeItem(i){const c=readCart();c.splice(i,1);saveCart(c);draw()}draw();
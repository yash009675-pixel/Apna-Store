const $=id=>document.getElementById(id);
function esc(v){return String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));}
async function bootSeller(){const {data,error}=await apnaSupabase.auth.getSession();if(error||!data.session){location.href="auth.html";return;}const user=data.session.user;const {data:profile,error:profileError}=await apnaSupabase.from("profiles").select("id,full_name,role").eq("id",user.id).single();if(profileError||!profile||!["seller","admin"].includes(profile.role)){location.href="account.html";return;} $("sellerTitle").textContent=(profile.full_name||user.email||"Seller")+" — Dashboard"; $("sellerSubtitle").textContent=profile.role==="admin"?"Admin catalog overview":"Manage your Apna Store catalog and inventory.";
const query=profile.role==="admin"?apnaSupabase.from("products").select("id,name,status,created_at,product_variants(count)").order("created_at",{ascending:false}):apnaSupabase.from("products").select("id,name,status,created_at,product_variants(count)").eq("seller_id",user.id).order("created_at",{ascending:false});
const {data:products,error:productsError}=await query;if(productsError){$("recentProducts").innerHTML='<div class="seller-message seller-error">Could not load products.</div>';return;}
const list=products||[];const ids=list.map(p=>p.id);let variants=[];if(ids.length){const {data:v}=await apnaSupabase.from("product_variants").select("id,product_id,stock").in("product_id",ids);variants=v||[];}
$("productCount").textContent=list.length;$("activeCount").textContent=list.filter(p=>p.status==="active").length;$("variantCount").textContent=variants.length;$("lowStockCount").textContent=variants.filter(v=>Number(v.stock)<=5).length;
if(!list.length){$("recentProducts").innerHTML='<div class="seller-message">No products yet. Start by adding your first product.</div>';return;}
$("recentProducts").innerHTML='<table class="seller-table"><thead><tr><th>Product</th><th>Status</th><th>Variants</th><th>Created</th></tr></thead><tbody>'+list.slice(0,8).map(p=>'<tr><td><strong>'+esc(p.name)+'</strong></td><td><span class="seller-status">'+esc(p.status)+'</span></td><td>'+variants.filter(v=>v.product_id===p.id).length+'</td><td>'+new Date(p.created_at).toLocaleDateString('en-IN')+'</td></tr>').join("")+'</tbody></table>';
}
$("signOut").onclick=async()=>{await apnaSupabase.auth.signOut();location.href="index.html";};
bootSeller();
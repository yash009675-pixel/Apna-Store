const $=id=>document.getElementById(id);const esc=v=>String(v??"").replace(/[&<>"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c]));async function load(){const{data,error}=await apnaSupabase.rpc("get_delivery_executive_dashboard");if(error){$("msg").textContent=error.message;return}const rows=data?.assignments||[];if(!rows.length){$("msg").textContent="No delivery assignments are currently available for this authenticated delivery account.";$("list").innerHTML="";return}$("msg").textContent="Assigned deliveries";$("list").innerHTML='<table class="seller-table"><thead><tr><th>Order</th><th>AWB</th><th>Status</th><th>Destination</th><th>ETA</th><th>Navigation</th></tr></thead><tbody>'+rows.map(r=>{const a=r.address||{};const dest=[a.address,a.city,a.state,a.pincode].filter(Boolean).join(", ");const nav=dest?"https://www.google.com/maps/search/?api=1&query="+encodeURIComponent(dest):"";return '<tr><td>'+esc(r.order_id)+'</td><td>'+esc(r.awb||"—")+'</td><td>'+esc(r.status)+'</td><td>'+esc(dest||"Not available")+'</td><td>'+esc(r.estimated_delivery_date||"—")+'</td><td>'+(nav?'<a class="seller-btn" target="_blank" rel="noopener" href="'+nav+'">Navigate</a>':"—")+'</td></tr>'}).join("")+'</tbody></table>'}$("refresh").onclick=load;load();
# Phase 76 — Data Exports\n\nFree-plan customer data export. Authenticated customers can download their own account data as JSON or CSV through `apna-data-export`. Includes account metadata, profile, addresses, orders, wishlists, reviews and notification preferences. Direct table access is revoked, JWT authentication is required, and export jobs are tracked and audit logged. No paid export service or external bulk-data endpoint was added.
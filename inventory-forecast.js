const $=id=>document.getElementById(id);const esc=v=>String(v??"").replace(/[&<>"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c]));async function load(){ $("table").textContent="Loading…";const {data,error}=await apnaSupabase.rpc("get_inventory_forecast",{p_days:Number($("days").value),p_forecast_days:30});if(error){$("table").textContent=error.message;return}const s=data?.summary||{};$("products").textContent=s.products??0;$("critical").textContent=s.critical??0;$("oos").textContent=s.out_of_stock??0;$("low").textContent=s.low??0;$("note").textContent=data?.seasonal_note||"";const rows=data?.products||[];if(!rows.length){$("table").textContent="No active products found.";return}$("table").innerHTML='<table class="seller-table"><thead><tr><th>Product</th><th>Stock</th><th>Sold</th><th>Daily demand</th><th>30-day forecast</th><th>Stock days</th><th>Reorder</th><th>Status</th></tr></thead><tbody>'+rows.map(r=>'<tr><td>'+esc(r.name)+'</td><td>'+r.current_stock+'</td><td>'+r.units_sold+'</td><td>'+r.avg_daily_demand+'</td><td>'+r.forecast_units+'</td><td>'+ (r.stock_days??"—") +'</td><td>'+r.reorder_suggestion+'</td><td><span class="seller-status">'+esc(r.status)+'</span></td></tr>').join("")+'</tbody></table>'}$("days").onchange=load;$("refresh").onclick=load;load();
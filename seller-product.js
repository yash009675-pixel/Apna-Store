const $=id=>document.getElementById(id);
const params=new URLSearchParams(location.search);
let editId=params.get("id"),user,role,productOwnerId=null,pendingImages=[],imageRows=[],variants=[];
const MAX_BYTES=5242880,MIN_DIM=400,MAX_DIM=6000,MAX_EDGE=2000;
function esc(v){return String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));}
function setMsg(message,error=false){$("msg").className=error?"msg error":"msg";$("msg").textContent=message||"";}
function variantOptions(selected){return '<select class="image-variant"><option value="">General product image</option>'+variants.map(v=>{const label=[v.size,v.color].filter(Boolean).join(" / ")||"Standard";return '<option value="'+esc(v.id)+'" '+(selected===v.id?"selected":"")+'>'+esc(label)+'</option>'}).join("")+'</select>';}
function validateFile(file){if(!/^image\/(jpeg|png|webp)$/.test(file.type))return"Only JPG, PNG or WebP images are supported.";if(file.size>MAX_BYTES)return"Each image must be 5 MB or smaller before optimization.";return"";}
function readDimensions(file){return new Promise((resolve,reject)=>{const url=URL.createObjectURL(file),img=new Image();img.onload=()=>{URL.revokeObjectURL(url);resolve({width:img.naturalWidth,height:img.naturalHeight,img})};img.onerror=()=>{URL.revokeObjectURL(url);reject(new Error("This image could not be read."))};img.src=url})}
async function prepareImage(file){const problem=validateFile(file);if(problem)throw new Error(problem);const meta=await readDimensions(file);if(meta.width<MIN_DIM||meta.height<MIN_DIM)throw new Error("Image must be at least "+MIN_DIM+"×"+MIN_DIM+" pixels.");if(meta.width>MAX_DIM||meta.height>MAX_DIM)throw new Error("Image dimensions cannot exceed "+MAX_DIM+"×"+MAX_DIM+" pixels.");const scale=Math.min(1,MAX_EDGE/meta.width,MAX_EDGE/meta.height),w=Math.max(1,Math.round(meta.width*scale)),h=Math.max(1,Math.round(meta.height*scale)),canvas=document.createElement("canvas");canvas.width=w;canvas.height=h;canvas.getContext("2d",{alpha:true}).drawImage(meta.img,0,0,w,h);const blob=await new Promise(resolve=>canvas.toBlob(resolve,"image/webp",.82));if(!blob)throw new Error("This browser could not optimize the image.");const base=file.name.replace(/\.[^.]+$/,"").replace(/[^a-z0-9_-]+/gi,"-").replace(/^-+|-+$/g,"")||"product-image";return{file:new File([blob],base+".webp",{type:"image/webp",lastModified:Date.now()}),width:w,height:h,originalName:file.name}}
function renderPending(){const box=$("previewList");box.innerHTML=pendingImages.map((p,i)=>'<div class="image-item preview-card"><img src="'+esc(p.previewUrl)+'" alt="'+esc(p.originalName)+'"><button type="button" class="preview-remove" data-pending-remove="'+i+'" aria-label="Remove image">×</button><div class="image-meta">'+esc(p.originalName)+'<br>'+p.width+"×"+p.height+" • optimized WebP</div>"+variantOptions(p.variantId||"")+"</div>").join("");box.querySelectorAll("[data-pending-remove]").forEach(b=>b.onclick=()=>{const i=Number(b.dataset.pendingRemove);URL.revokeObjectURL(pendingImages[i].previewUrl);pendingImages.splice(i,1);renderPending()});box.querySelectorAll(".image-variant").forEach((s,i)=>s.onchange=()=>pendingImages[i].variantId=s.value||null)}
function renderImages(){const box=$("imageList");if(!imageRows.length){box.innerHTML='<div class="empty-images">No uploaded product images yet.</div>';return}box.innerHTML=imageRows.map((x,i)=>{const u=apnaSupabase.storage.from("product-images").getPublicUrl(x.storage_path).data.publicUrl;return '<div class="image-item" draggable="true" data-image-card="'+x.id+'"><img src="'+esc(u)+'" alt="'+esc(x.alt_text||$("name").value.trim())+'"><div class="image-meta">Image '+(i+1)+(x.is_primary?" • Primary":"")+'</div>'+variantOptions(x.variant_id||"")+'<div class="image-controls"><button type="button" data-up="'+x.id+'" '+(i===0?"disabled":"")+' title="Move up">↑</button><button type="button" data-down="'+x.id+'" '+(i===imageRows.length-1?"disabled":"")+' title="Move down">↓</button><button type="button" class="danger" data-delete="'+x.id+'">Delete</button></div><label class="primary-mark"><input type="radio" name="primaryImage" '+(x.is_primary?"checked":"")+' data-primary="'+x.id+'"> Primary image</label></div>'}).join("");
box.querySelectorAll("[data-primary]").forEach(el=>el.onchange=async()=>setPrimary(el.dataset.primary));box.querySelectorAll("[data-delete]").forEach(el=>el.onclick=()=>deleteImage(el.dataset.delete));box.querySelectorAll("[data-up]").forEach(el=>el.onclick=()=>moveImage(el.dataset.up,-1));box.querySelectorAll("[data-down]").forEach(el=>el.onclick=()=>moveImage(el.dataset.down,1));box.querySelectorAll(".image-variant").forEach((s,i)=>s.onchange=async()=>{const r=imageRows[i];const{error}=await apnaSupabase.from("product_images").update({variant_id:s.value||null}).eq("id",r.id);if(error)setMsg(error.message,true);else{r.variant_id=s.value||null;setMsg("Image variant link updated.")}});box.querySelectorAll("[data-image-card]").forEach(card=>{card.ondragstart=e=>{e.dataTransfer.setData("text/plain",card.dataset.imageCard)};card.ondragover=e=>e.preventDefault();card.ondrop=async e=>{e.preventDefault();const from=e.dataTransfer.getData("text/plain");await reorderTo(from,card.dataset.imageCard)}})}
async function setPrimary(id){const{error}=await apnaSupabase.from("product_images").update({is_primary:false}).eq("product_id",editId);if(error){setMsg(error.message,true);return}const r=await apnaSupabase.from("product_images").update({is_primary:true}).eq("id",id);if(r.error){setMsg(r.error.message,true);return}await loadImages();setMsg("Primary image updated.")}
async function moveImage(id,delta){const i=imageRows.findIndex(x=>x.id===id),j=i+delta;if(i<0||j<0||j>=imageRows.length)return;await reorderTo(id,imageRows[j].id)}
async function reorderTo(fromId,toId){const from=imageRows.findIndex(x=>x.id===fromId),to=imageRows.findIndex(x=>x.id===toId);if(from<0||to<0||from===to)return;const next=[...imageRows],item=next.splice(from,1)[0];next.splice(to,0,item);for(let k=0;k<next.length;k++){const r=await apnaSupabase.from("product_images").update({sort_order:k}).eq("id",next[k].id);if(r.error){setMsg(r.error.message,true);return}}await loadImages()}
async function deleteImage(id){const row=imageRows.find(x=>x.id===id);if(!row)return;setMsg("Deleting image…");const s=await apnaSupabase.storage.from("product-images").remove([row.storage_path]);if(s.error){setMsg(s.error.message,true);return}const d=await apnaSupabase.from("product_images").delete().eq("id",id);if(d.error){setMsg(d.error.message,true);return}if(row.is_primary){const{data:remaining}=await apnaSupabase.from("product_images").select("id").eq("product_id",editId).order("sort_order").limit(1);if(remaining?.[0])await setPrimary(remaining[0].id)}await loadImages();setMsg("Image deleted.")}
async function loadImages(){if(!editId){renderPending();return}const{data,error}=await apnaSupabase.from("product_images").select("id,storage_path,alt_text,sort_order,is_primary,variant_id").eq("product_id",editId).order("sort_order");if(error){setMsg(error.message,true);return}imageRows=data||[];renderImages()}
async function loadVariants(){if(!editId)return;const{data,error}=await apnaSupabase.from("product_variants").select("id,size,color,sku,stock").eq("product_id",editId).order("created_at");if(error){setMsg(error.message,true);return}variants=data||[];renderImages();renderPending()}
function addVariant(v={}){const row=document.createElement("div");row.className="variant";row.dataset.variantId=v.id||"";row.innerHTML='<span class="variant-id">'+(v.id?"Saved":"New")+'</span><input class="vsize" placeholder="Size" value="'+esc(v.size||"")+'"><input class="vcolor" placeholder="Color" value="'+esc(v.color||"")+'"><input class="vsku" placeholder="SKU" value="'+esc(v.sku||"")+'"><input class="vstock" type="number" min="0" placeholder="Stock" value="'+(v.stock??0)+'"><button type="button" class="btn remove">Remove</button>';row.querySelector(".remove").onclick=()=>row.remove();$("variantRows").appendChild(row)}
async function uploadPreparedImage(prepared,variantId=null){const path=(productOwnerId||user.id)+"/"+editId+"/"+crypto.randomUUID()+".webp";const up=await apnaSupabase.storage.from("product-images").upload(path,prepared.file,{cacheControl:"31536000",upsert:false,contentType:"image/webp"});if(up.error)throw up.error;const{data:imgs}=await apnaSupabase.from("product_images").select("id,sort_order").eq("product_id",editId).order("sort_order",{ascending:false}).limit(1);const next=(imgs?.[0]?.sort_order??-1)+1;const ins=await apnaSupabase.from("product_images").insert({product_id:editId,variant_id:variantId||null,storage_path:path,alt_text:$("name").value.trim(),sort_order:next,is_primary:!imageRows.length});if(ins.error){await apnaSupabase.storage.from("product-images").remove([path]);throw ins.error}}
async function uploadPendingImages(){if(!editId||!pendingImages.length)return true;setMsg("Optimizing and uploading "+pendingImages.length+" image(s)…");let ok=true;for(const p of pendingImages){try{await uploadPreparedImage(p,p.variantId||null)}catch(e){ok=false;setMsg(e.message||"Image upload failed.",true)}URL.revokeObjectURL(p.previewUrl)}pendingImages=[];await loadImages();renderPending();return ok}
async function handleFiles(files){const selected=[...files];if(!selected.length)return;let accepted=0;for(const file of selected){try{const prepared=await prepareImage(file);prepared.previewUrl=URL.createObjectURL(prepared.file);pendingImages.push(prepared);accepted++}catch(e){setMsg(file.name+": "+(e.message||"Invalid image."),true)}}renderPending();if(accepted)setMsg(accepted+" image(s) ready. "+(editId?"Uploading now.":"Save the listing to upload them."));if(editId&&accepted)await uploadPendingImages()}
function num(id){const v=$(id).value.trim();return v===""?null:Number(v)}
function jsonValue(id,defaultValue){const raw=$(id).value.trim();if(!raw)return defaultValue;try{return JSON.parse(raw)}catch(e){throw new Error($(id).previousElementSibling?.textContent+" must be valid JSON.")}}
function collectVariants(){return[...document.querySelectorAll(".variant")].map(r=>({id:r.dataset.variantId||null,size:r.querySelector(".vsize").value.trim()||null,color:r.querySelector(".vcolor").value.trim()||null,sku:r.querySelector(".vsku").value.trim()||null,stock:Math.max(0,Number(r.querySelector(".vstock").value)||0)}))}
function preview(){const first=imageRows[0];const img=first?apnaSupabase.storage.from("product-images").getPublicUrl(first.storage_path).data.publicUrl:"";const attrs=(()=>{try{return JSON.parse($("attributes").value||"{}")}catch{return{}}})();$("preview").hidden=false;$("preview").innerHTML='<div class="preview-grid">'+(img?'<img src="'+esc(img)+'" alt="">':'<div></div>')+'<div><h2>'+esc($("name").value||"Untitled product")+'</h2><p>'+moneyPreview($("price").value)+' '+($("compare").value?"<s>"+moneyPreview($("compare").value)+"</s>":"")+'</p><p>'+esc($("description").value||"No description yet.")+'</p><p><strong>Category:</strong> '+esc($("category").selectedOptions[0]?.textContent||"")+' · <strong>Brand:</strong> '+esc($("brand").selectedOptions[0]?.textContent||"No brand")+'</p><p><strong>Variants:</strong> '+collectVariants().length+' · <strong>GST:</strong> '+esc($("gst").value||"—")+'%</p><p><strong>Attributes:</strong> '+esc(JSON.stringify(attrs))+'</p></div></div>';window.scrollTo({top:$("preview").offsetTop-80,behavior:"smooth"})}
function moneyPreview(v){return"₹"+Number(v||0).toLocaleString("en-IN",{maximumFractionDigits:2})}
async function saveProduct(forceStatus=null){$("save").disabled=true;$("submitApproval").disabled=true;setMsg("Saving listing…");try{const name=$("name").value.trim();if(!name)throw new Error("Please enter a product name.");const attrs=jsonValue("attributes",{}),shipping=jsonValue("shipping",{});const status=forceStatus||$("status").value;const payload={p_product_id:editId||null,p_name:name,p_description:$("description").value.trim(),p_price:Number($("price").value),p_compare_at_price:num("compare"),p_discount_percent:num("discount"),p_status:status,p_category_id:$("category").value||null,p_subcategory:$("subcategory").value.trim(),p_brand_id:$("brand").value||null,p_attributes:attrs,p_gst_rate:num("gst"),p_shipping_details:shipping,p_return_policy:$("returnPolicy").value.trim(),p_package_weight_kg:num("weight"),p_package_length_cm:num("length"),p_package_width_cm:num("width"),p_package_height_cm:num("height"),p_variants:collectVariants()};const{data:savedId,error}=await apnaSupabase.rpc("seller_save_listing",payload);if(error)throw error;editId=savedId||editId;if(!editId)throw new Error("Listing was saved but its ID could not be confirmed.");const ownerRes=await apnaSupabase.from("products").select("seller_id").eq("id",editId).single();if(ownerRes.error||!ownerRes.data?.seller_id)throw ownerRes.error||new Error("Product owner could not be confirmed.");productOwnerId=ownerRes.data.seller_id;$("heading").textContent="Edit listing";await loadVariants();const uploaded=await uploadPendingImages();await loadImages();$("status").value=status;setMsg(uploaded?(status==="pending_approval"?"Listing submitted for approval.":"Listing saved successfully."):"Listing saved, but one or more images need attention.",!uploaded);return true}catch(err){setMsg(err.message||"Could not save listing.",true);return false}finally{$("save").disabled=false;$("submitApproval").disabled=false}}
async function boot(){const{data:s,error}=await apnaSupabase.auth.getSession();if(error||!s.session){location.href="auth.html";return}user=s.session.user;const{data:p}=await apnaSupabase.from("profiles").select("role").eq("id",user.id).single();if(!p||!["seller","admin"].includes(p.role)){location.href="account.html";return}role=p.role;const[catRes,brandRes]=await Promise.all([apnaSupabase.from("categories").select("id,name").eq("is_active",true).order("sort_order").order("name"),apnaSupabase.from("brands").select("id,name").eq("is_active",true).order("sort_order").order("name")]);if(catRes.error||brandRes.error){setMsg((catRes.error||brandRes.error).message,true);return}$("category").innerHTML='<option value="">Select category</option>'+(catRes.data||[]).map(c=>'<option value="'+c.id+'">'+esc(c.name)+'</option>').join("");$("brand").innerHTML='<option value="">No brand</option>'+(brandRes.data||[]).map(b=>'<option value="'+b.id+'">'+esc(b.name)+'</option>').join("");
if(editId){$("heading").textContent="Edit listing";const{data:prod,error}=await apnaSupabase.from("products").select("id,name,description,price,compare_at_price,discount_percent,status,category_id,subcategory,brand_id,attributes,gst_rate,shipping_details,return_policy,package_weight_kg,package_length_cm,package_width_cm,package_height_cm,seller_id,rejection_reason").eq("id",editId).single();if(error||!prod||(role==="seller"&&prod.seller_id!==user.id)){location.href="seller-products.html";return}productOwnerId=prod.seller_id;$("name").value=prod.name||"";$("description").value=prod.description||"";$("price").value=prod.price??"";$("compare").value=prod.compare_at_price??"";$("discount").value=prod.discount_percent??"";$("gst").value=prod.gst_rate??"";$("status").value=prod.status||"draft";$("category").value=prod.category_id||"";$("subcategory").value=prod.subcategory||"";$("brand").value=prod.brand_id||"";$("attributes").value=JSON.stringify(prod.attributes||{},null,2);$("shipping").value=JSON.stringify(prod.shipping_details||{},null,2);$("returnPolicy").value=prod.return_policy||"";$("weight").value=prod.package_weight_kg??"";$("length").value=prod.package_length_cm??"";$("width").value=prod.package_width_cm??"";$("height").value=prod.package_height_cm??"";if(prod.rejection_reason)setMsg("Rejection reason: "+prod.rejection_reason,true);await loadVariants();await loadImages()}else addVariant();
$("addVariant").onclick=()=>addVariant();$("previewBtn").onclick=preview;$("submitApproval").onclick=()=>saveProduct("pending_approval");const dz=$("dropzone");dz.onclick=()=>$("images").click();dz.onkeydown=e=>{if(e.key==="Enter"||e.key===" "){e.preventDefault();$("images").click()}};dz.ondragover=e=>{e.preventDefault();dz.classList.add("drag")};dz.ondragleave=()=>dz.classList.remove("drag");dz.ondrop=e=>{e.preventDefault();dz.classList.remove("drag");handleFiles(e.dataTransfer.files)};$("images").onchange=()=>handleFiles($("images").files)}
$("form").addEventListener("submit",async e=>{e.preventDefault();await saveProduct()});boot();
const list=document.getElementById("ordersList");
function money(n){return "₹"+Number(n||0).toLocaleString("en-IN")}
function esc(v){return String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]))}
function renderOrders(orders,cloud){list.innerHTML=orders.length?orders.map(o=>{const items=o.order_items||o.items||[];const subtotal=Number(o.subtotal??items.reduce((s,x)=>s+Number(x.unit_price??x.price||0)*Number(x.quantity||x.qty||1),0));const delivery=Number(o.delivery_fee??o.delivery??0);const total=Number(o.total??subtotal+delivery);const date=o.created_at||o.createdAt;return '<article class="checkout-card"><p class="eyebrow">ORDER</p><h2>'+esc(o.order_number||o.id||"Order")+'</h2><div class="summary-line"><span>Status</span><b>'+esc(o.status||"placed")+'</b></div><div class="summary-line"><span>Payment status</span><b>'+esc(o.payment_status||o.paymentStatus||"pending")+'</b></div>'+items.map(x=>'<div class="summary-line"><span>'+esc(x.product_name||x.name||"Product")+' × '+Number(x.quantity||x.qty||1)+'<small>Price: '+money(x.unit_price??x.price)+'</small></span><b>'+money(Number(x.unit_price??x.price)*Number(x.quantity||x.qty||1))+'</b></div>').join("")+'<div class="summary-line"><span>Subtotal</span><b>'+money(subtotal)+'</b></div><div class="summary-line"><span>Delivery</span><b>'+money(delivery)+'</b></div><div class="summary-total"><span>Total</span><b>'+money(total)+'</b></div><p class="checkout-note">Order date: '+esc(new Date(date).toLocaleString("en-IN"))+'</p></article>'}).join(""):'<div class="empty-cart"><h2>No orders yet.</h2><p>'+(cloud?"Your account orders will appear here.":"Your completed guest orders are saved on this device.")+'</p><a class="primary-btn" href="shop.html">Start shopping →</a></div>'}
(async()=>{const {data:{session}}=await apnaSupabase.auth.getSession();if(session){const {data,error}=await apnaSupabase.from("orders").select("id,order_number,status,payment_status,subtotal,delivery_fee,total,shipping_address,created_at,order_items(product_name,unit_price,quantity)").eq("user_id",session.user.id).order("created_at",{ascending:false});if(error){console.error(error);list.innerHTML='<div class="empty-cart"><h2>Could not load orders.</h2><p>Please refresh and try again.</p></div>';return}renderOrders(data||[],true)}else{let orders=[];try{orders=JSON.parse(localStorage.getItem("apnaOrders")||"[]")}catch{}renderOrders(Array.isArray(orders)?orders:[],false)}})();
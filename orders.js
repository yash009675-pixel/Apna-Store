const list=document.getElementById("ordersList"),orders=JSON.parse(localStorage.getItem("apnaOrders")||"[]");function money(n){return "₹"+Number(n).toLocaleString("en-IN")}list.innerHTML=orders.length?orders.map(o=>'<article class="checkout-card"><p class="eyebrow">'+o.id+'</p><h2>'+new Date(o.createdAt).toLocaleString("en-IN")+'</h2>'+o.items.map(x=>'<div class="summary-line"><span>'+x.name+' × '+x.qty+'</span><b>'+money(x.price*x.qty)+'</b></div>').join("")+'<div class="summary-total"><span>Total</span><b>'+money(o.total)+'</b></div></article>').join(""):'<div class="empty-cart"><h2>No orders yet.</h2><p>Your completed demo orders will appear here on this device.</p><a class="primary-btn" href="shop.html">Start shopping →</a></div>';
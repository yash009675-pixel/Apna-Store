const $=id=>document.getElementById(id);const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));const money=n=>"₹"+Number(n||0).toLocaleString("en-IN");async function load(){const {data:s}=await apnaSupabase.auth.getSession();if(!s.session){location.href="auth.html";return;}const {data,error}=await apnaSupabase.rpc("get_seller_orders",{p_status:$("status").value||null,p_delivery_status:$("delivery").value||null});if(error){$("message").textContent=error.message;return;}const rows=data||[];$("message").textContent=rows.length+" order(s)";$("panel").innerHTML=!rows.length?"<p>No matching orders.</p>":'<div style="overflow:auto"><table style="width:100%;border-collapse:collapse"><thead><tr><th>Order</th><th>Items</th><th>Total</th><th>Payment</th><th>Order status</th><th>Delivery</th><th>Action</th></tr></thead><tbody>'+rows.map(o=>'<tr><td><strong>'+esc(o.order_number)+'</strong><br>'+new Date(o.created_at).toLocaleDateString("en-IN")+'</td><td>'+o.items.map(i=>esc(i.product_name)+" × "+Number(i.quantity)).join("<br>")+'</td><td>'+money(o.seller_total)+'</td><td>'+esc(o.payment_method==="cod"?"COD":o.payment_status||"Online")+'</td><td>'+esc(o.status)+'</td><td>'+esc(o.delivery_status)+'</td><td><button data-id="'+o.id+'" class="manage">Manage</button></td></tr>').join("")+'</tbody></table></div>';$("panel").querySelectorAll(".manage").forEach(b=>b.onclick=()=>manage(b.dataset.id,rows));}async function manage(id,rows){const o=rows.find(x=>x.id===id);const status=prompt("Order status (pending/confirmed/processing/shipped/delivered/cancelled)",o.status);if(status===null)return;const delivery=prompt("Delivery status (pending/processing/shipped/out_for_delivery/delivered/cancelled/returned)",o.delivery_status);if(delivery===null)return;const provider=prompt("Tracking provider (optional)",o.tracking_provider||"");const number=prompt("Tracking number (optional)",o.tracking_number||"");const {error}=await apnaSupabase.rpc("update_seller_order_status",{p_order_id:id,p_status:status,p_delivery_status:delivery,p_tracking_provider:provider,p_tracking_number:number});if(error)alert(error.message);else load();}$("reload").onclick=load;$("status").onchange=load;$("delivery").onchange=load;load();
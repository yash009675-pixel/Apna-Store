let audience="customer",articles=[];const $=id=>document.getElementById(id);function esc(v){return String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]))}async function load(){const {data,error}=await apnaSupabase.from("help_articles").select("id,audience,category,title,body,sort_order").eq("audience",audience).eq("is_published",true).order("sort_order").order("title");if(error){$("helpGrid").innerHTML='<div class="help-empty">Help articles could not be loaded. Please refresh.</div>';return}articles=data||[];const cats=[...new Set(articles.map(x=>x.category))];$("helpCategory").innerHTML='<option value="">All categories</option>'+cats.map(x=>'<option value="'+esc(x)+'">'+esc(x)+'</option>').join("");render()}function render(){const q=$("helpSearch").value.trim().toLowerCase(),cat=$("helpCategory").value;const list=articles.filter(x=>(!cat||x.category===cat)&&(!q||[x.title,x.body,x.category].join(" ").toLowerCase().includes(q)));$("helpGrid").innerHTML=list.length?list.map(x=>'<article class="help-card"><small>'+esc(x.category)+'</small><h3>'+esc(x.title)+'</h3><p>'+esc(x.body)+'</p></article>').join(""):'<div class="help-empty">No matching help articles found.</div>'}document.querySelectorAll(".help-tab").forEach(b=>b.onclick=()=>{document.querySelectorAll(".help-tab").forEach(x=>x.classList.remove("active"));b.classList.add("active");audience=b.dataset.audience;load()});$("helpSearch").oninput=render;$("helpCategory").onchange=render;load();